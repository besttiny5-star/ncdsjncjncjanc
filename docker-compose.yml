version: "3.9"

services:
  bot:
    build: .
    container_name: payment-qa-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      BOT_DB_PATH: /app/data/orders.db
    volumes:
      - ./data:/app/data
    command: ["python", "bot.py"]
